---
description: >
  Directly select Verify's formal-library search for reusable mathematical
  theorems and lemmas. Use only when invoked explicitly by the user.
disable-model-invocation: true
argument-hint: "<mathematical result or concept to find>"
---

# Verify retrieval

1. Read `../verify/SKILL.md` and follow all of its runtime, trust, and reporting
   rules.
2. Select `../verify/workflows/retrieve/WORKFLOW.md` directly.
3. Treat search hits as candidates until their types and hypotheses match.
4. Use the bundled plugin runtime and product tools. Never assume the user's
   current project is a clone of Verify Harness.

The user's target is:

$ARGUMENTS
