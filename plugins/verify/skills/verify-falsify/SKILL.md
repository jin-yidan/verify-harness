---
description: >
  Directly select Verify's counterexample and falsification workflow for a
  mathematical claim. Use only when invoked explicitly by the user.
disable-model-invocation: true
argument-hint: "<claim, proof step, file, paper, or URL>"
---

# Verify falsification

1. Read `../verify/SKILL.md` and follow all of its runtime, trust, and reporting
   rules.
2. Select `../verify/workflows/falsify/WORKFLOW.md` directly.
3. Keep the run limited to falsification unless the user later requests
   another workflow.
4. Use the bundled plugin runtime and product tools. Never assume the user's
   current project is a clone of Verify Harness.

The user's target is:

$ARGUMENTS
