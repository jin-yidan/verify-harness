---
description: >
  Directly select Verify's formal-statement versus intended-meaning audit.
  Use only when invoked explicitly by the user.
disable-model-invocation: true
argument-hint: "<formal statement and intended mathematical meaning>"
---

# Verify statement

1. Read `../verify/SKILL.md` and follow all of its runtime, trust, and reporting
   rules.
2. Select `../verify/workflows/statement/WORKFLOW.md` directly.
3. Limit the result to statement fidelity; do not claim the theorem is proved.
4. Use the bundled plugin runtime and product tools. Never assume the user's
   current project is a clone of Verify Harness.

The user's target is:

$ARGUMENTS
