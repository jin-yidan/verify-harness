---
description: >
  Directly select Verify's inexpensive sealed review and suspicious-step
  triage workflow. Use only when invoked explicitly by the user.
disable-model-invocation: true
argument-hint: "<proof, derivation, file, paper, or URL>"
---

# Verify triage

1. Read `../verify/SKILL.md` and follow all of its runtime, trust, and reporting
   rules.
2. Select `../verify/workflows/triage/WORKFLOW.md` directly.
3. Treat triage as prioritization evidence only, never as a correctness result.
4. Use the bundled plugin runtime and product tools. Never assume the user's
   current project is a clone of Verify Harness.

The user's target is:

$ARGUMENTS
