interface:
  display_name: "Verify Library"
  short_description: "Add verified lemmas to the corpus"
  default_prompt: "Use $verify-library to run the corresponding RLVerify command."

policy:
  allow_implicit_invocation: true
